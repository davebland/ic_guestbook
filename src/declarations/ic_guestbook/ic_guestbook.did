service : {
  addEntry: (text) -> (text);
  addVisit: () -> ();
  getEntries: () -> (vec record {
                           text;
                           int;
                         }) query;
  getVisitorCount: () -> (nat) query;
  resetGuestBook: () -> ();
}
