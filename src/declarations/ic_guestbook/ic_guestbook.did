service : {
  addEntry: (text) -> (text);
  getEntries: () -> (vec record {
                           text;
                           int;
                         }) query;
  resetGuestBook: () -> ();
}
